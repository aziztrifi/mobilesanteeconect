<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/blog_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp">

        <!-- ConstraintLayout inside the CardView for organizing child views -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp">

            <!-- Blog Title -->
            <TextView
                android:id="@+id/blogTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="Blog Title"
                android:textColor="#000"
                android:textSize="16sp"
                android:textStyle="bold"
                app:layout_constraintStart_toEndOf="@id/authorProfilePicture"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="Blog Title Example" />

            <!-- Author Name -->
            <TextView
                android:id="@+id/authorName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_marginTop="4dp"
                android:text="User Name"
                android:textColor="#000"
                android:textSize="14sp"
                app:layout_constraintStart_toEndOf="@id/authorProfilePicture"
                app:layout_constraintTop_toBottomOf="@id/blogTitle"
                tools:text="Author Name" />

            <!-- Blog Date -->
            <TextView
                android:id="@+id/blogDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Date"
                android:textColor="#666"
                android:textSize="12sp"
                app:layout_constraintStart_toEndOf="@id/authorProfilePicture"
                app:layout_constraintTop_toBottomOf="@id/authorName"
                tools:text="13-11-2024" />

            <!-- Author Profile Picture -->
            <ImageView
                android:id="@+id/authorProfilePicture"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="Author Profile Picture"
                android:src="@drawable/az"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="@id/blogDate"
                app:layout_constraintVertical_bias="0.0" />

            <!-- Blog Description -->
            <TextView
                android:id="@+id/blogDescription"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Blog description here"
                android:textColor="#666"
                android:textSize="14sp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@id/authorProfilePicture"
                tools:text="This is a short description of the blog post." />

            <!-- Blog Image -->
            <androidx.cardview.widget.CardView
                android:id="@+id/blog_image_card"
                android:layout_width="match_parent"
                android:layout_height="150dp"
                android:layout_marginTop="8dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="4dp"
                app:layout_constraintTop_toBottomOf="@id/blogDescription">

                <ImageView
                    android:id="@+id/blogImageView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="Blog Image"
                    android:scaleType="centerCrop"
                    tools:src="@drawable/ic_search" />

            </androidx.cardview.widget.CardView>

            <!-- Delete Button -->
            <ImageButton
                android:id="@+id/deleteButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginEnd="8dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@id/blog_image_card"
                app:srcCompat="@android:drawable/ic_delete"
                android:contentDescription="Delete Blog" />

            <!-- Show More Button -->
            <ImageView
                android:id="@+id/show"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginEnd="8dp"
                app:layout_constraintEnd_toStartOf="@id/deleteButton"
                app:layout_constraintTop_toBottomOf="@id/blog_image_card"
                app:srcCompat="@drawable/ic_more"
                android:contentDescription="Show More" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>
